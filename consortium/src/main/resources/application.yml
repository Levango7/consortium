spring:
  datasource:
    url: 'jdbc:postgresql://localhost:5432/your-data-base' # SPRING_DATASOURCE_URL
    username: 'username'
    password: 'password'

#  jpa:
#    database-platform: org.hibernate.dialect.MySQL5Dialect
#    hibernate:
#      ddl-auto: create-drop # create, update
#    show-sql: true # enable jpa sql logs



# 创世区块文件
consortium.consensus.genesis: 'genesis/wisdom-genesis-generator.json'


server:
  port: '8080'

logging:
  level:
    org.hibernate:
      SQL: 'DEBUG'
      type.descriptor.sql.BasicBinder: 'TRACE'
    org.springframework.jdbc.core:
      JdbcTemplate: 'DEBUG'
      StatementCreatorUtils: 'TRACE'
